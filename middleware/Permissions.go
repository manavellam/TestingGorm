package middleware

import "github.com/gin-gonic/gin"

//CheckPermissions checks if user has the required permission for certain endpoints.
func CheckPermissions() gin.HandlerFunc {
	return func(c *gin.Context) {

	}
}
